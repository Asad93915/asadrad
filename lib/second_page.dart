import 'package:flutter/material.dart';

class SecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("My Second Page",style: TextStyle(color: Colors.black,fontSize: 20.0
        ),
        ),
      ),
    );
  }
}
